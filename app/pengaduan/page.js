import PlaceholderPage from "@/components/PlaceholderPage";
import { placeholderPages } from "@/lib/content";

export default function PengaduanPage() {
  return <PlaceholderPage {...placeholderPages.pengaduan} />;
}
